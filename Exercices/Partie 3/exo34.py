# Quelle est la différence entre `import nom_module` et `from nom_module import fonction`?

# import nom_module importe tout le module, alors que from nom_module import fonction importe seulement la fonction du module.